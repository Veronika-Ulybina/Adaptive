
/*let clientWidth = document.body.clientWidth;

$('.benefits__row').slick({
  speed: 500,
  infinite: false,
  draggable: false,
  waitForAnimate: false,
});

function resizeTaleSlider(clientWidth) {
  if (clientWidth < 480) {
    $('.benefits__row').slick({
      speed: 500,
      infinite: false,
      draggable: false,
      waitForAnimate: false,
    });
  } else {
    $('.benefits__row').slick("unslick");
  }
}

resizeTaleSlider(clientWidth);

$(window).resize(function() {
  let clientWidth = document.body.clientWidth;
  resizeTaleSlider(clientWidth);
});*/







